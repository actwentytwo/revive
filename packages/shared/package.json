{
  "name": "@revolution/shared",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "lint:fix": "pnpm run lint",
    "test": "node -e \"console.log('No shared tests configured yet')\"",
    "test:unit": "node -e \"console.log('No shared unit tests configured yet')\"",
    "test:integration": "node -e \"console.log('No shared integration tests configured yet')\""
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
